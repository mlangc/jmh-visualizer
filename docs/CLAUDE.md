# docs/

This is the live GitHub Pages publish root for `jmh.morethan.io` (see
`CNAME`), tracked in git — every "Release x.y.z" commit touches it. It's
populated by `npm run deploy` (`cp -r build/* docs/`), never hand-edited.

Let's not touch this for the time being.